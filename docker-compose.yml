services:
  share-it:
    build: .
    container_name: share-it
    restart: unless-stopped
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${HOST_PORT:-3050}:3050"
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
