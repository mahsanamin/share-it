services:
  share-it:
    build: .
    container_name: share-it
    restart: unless-stopped
    ports:
      - "127.0.0.1:3050:3050"
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
